// Definir la función converterAMaiusculas
function converterAMaiusculas(cadena) {
    return cadena.toUpperCase();
}

// Exportar la función para que esté disponible fuera del módulo
module.exports = converterAMaiusculas;
